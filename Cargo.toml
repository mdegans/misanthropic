[workspace]
resolver = "2"
members = ["misanthropic", "chat"]

[profile.release]
lto = true
strip = true
# There are (hopefully) no panics in this library. If there are, they are bugs.
panic = "abort"
