/target
/target-linux
# Cargo.lock is intentionally NOT ignored here: the bashd image builds with
# `cargo build --locked`, which needs the committed lockfile in the context.
.vscode
cobertura.xml
api.key
lcov.info
node_modules/
Secrets.toml
*.Dockerfile
*.docker-compose.yaml
*.env